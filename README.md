# NewGenStream
A PostgreSQL database project modeling a streaming platform (users, subscriptions, content catalog, cast, production contracts, and watch history), complete with E-R diagram, schema design, 8 SQL queries, index optimization, and a C CLI app to interact with the DB.
